export declare class HealthStatusModel {
    readonly status: string;
    readonly timestamp: string;
    readonly uptimeSeconds: number;
    constructor(status: string, timestamp: string, uptimeSeconds: number);
}
